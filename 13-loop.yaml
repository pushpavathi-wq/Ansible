#- name: print items
 # hosts: web

 # tasks:
   # - name: items
   #   ansible.builtin.debug:
   #    msg: "Hey {{ item }}"

    #  loop:
      #  - pushpa
      #  - prasad 


- name: loops printing
  hosts: web
  become: yes

  tasks:
    - name: install packages
      ansible.builtin.package:
       name: "{{ item }}"
       state: installed

      loop:
        - git
        - ansible
