- name: file variables
  hosts: web
  #connection: local


  tasks:
    - name: course information
      ansible.builtin.debug:
        msg: course is {{ COURSE }}, duration is {{ DURATION }} 


        