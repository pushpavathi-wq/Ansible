- name: undefined variables
  hosts: localhost
  connection: local

  tasks:
    - name: print default variables
      ansible.builtin.debug:
        msg: "Hi {{ pushpa | default ('female') }}"



    - name: convert string to list
      vars:
        fruitname: "'mango','banana'"
      ansible.builtin.debug:
        msg: "selling fruits: {{ fruitname }}"      